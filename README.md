# 📊 Moringa CX-Insight Toolkit
This project uses Python and NLP (Natural Language Processing) to analyze customer survey feedback.

## 🚀 How to Run
1. Clone this repo.
2. Run `pip install textblob`.
3. Run `python -m textblob.download_corpora`.
4. Run `python main.py`.